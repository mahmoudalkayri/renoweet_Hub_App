# Renoweet Focus Hub v3.2

OS → Bookkeeping queue improvement.

- “Send to bookkeeping” now queues the invoice with project/customer metadata and immediately performs a verified Drive save when connected.
- Bookkeeping button shows the real number of OS invoices waiting to import.
- Import copies invoice + project/customer metadata, prevents duplicate creation by invoice number, updates changed queued invoices, and verifies the Drive save.
- XLSX remains archive/recovery only.
